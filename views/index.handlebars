<div class="container">
    <div class="jumbotron bg-dark text-center">
        <i class="fas fa-hamburger"></i><br>
        <h1>Eat-Da-Burger!</h1>
    </div>

    {{#each burgers}}
    <span>{{this.id}}.</span>
    {{this.burger_name}}
    {{this.devoured}}
    <br>
    {{/each}}

</div>